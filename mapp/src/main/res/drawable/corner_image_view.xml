<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">    <!-- 填充的颜色 -->
    <solid android:color="@color/base_backgroundcolor" />    <!-- 设置按钮的四个角为弧形 -->    <!-- android:radius 弧形的半径 -->
    <corners
        android:topLeftRadius="10dp"
        android:topRightRadius="10dp"
        android:bottomLeftRadius="10dp"
        android:bottomRightRadius="10dp"/>
    <gradient
        android:angle="270"
        android:centerColor="@color/base_backgroundcolor"
        android:endColor="@color/base_backgroundcolor"
        android:startColor="@color/base_backgroundcolor" />
</shape>